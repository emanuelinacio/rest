<h1>Listing trackers</h1>

<table>
  <thead>
    <tr>
      <th>Users</th>
      <th>Date tracker</th>
      <th>Lat tracker</th>
      <th>Long tracker</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @trackers.each do |tracker| %>
      <tr>
        <td><%= tracker.users %></td>
        <td><%= tracker.date_tracker %></td>
        <td><%= tracker.lat_tracker %></td>
        <td><%= tracker.long_tracker %></td>
        <td><%= link_to 'Show', tracker %></td>
        <td><%= link_to 'Edit', edit_tracker_path(tracker) %></td>
        <td><%= link_to 'Destroy', tracker, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Tracker', new_tracker_path %>
